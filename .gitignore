node_modules/
.env
dist/
dist-ssr/
.DS_Store
*.local
.firebase/
.firebaserc
*.zip
server/serviceAccountKey.json
server/config/*.json
server/config/*.js
server/temp_backups/

# Database dumps and one-off destructive maintenance scripts. These touch real
# production records — never commit them. `artifacts/` was previously untracked
# but not ignored, so a `git add .` would have committed a full production dump.
artifacts/
*_db_backup_*.json
server/delete_production.js
server/dump_production.js
*.log
firebase-debug.log


OpenWA/

# Server sensitive data — never commit local DB or production dumps
server/data/
server/cashbook_test_out.json
server/vouchers_dump_test.json
server/online_adv_out.json

# Environment files — all variants
.env.*
!.env.example
!.env.beta.example

# Operational scripts (contain schema/prod details)
server/matches.txt

# Android build artifacts
android-terminal/.gradle/
android-terminal/build/
android-terminal/*/build/
android-terminal/local.properties
android-terminal/nul

