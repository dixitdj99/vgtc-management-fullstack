# Client Environment — PRODUCTION (Netlify Main Deploy)
# Vite bakes this value into the bundle at build time.
# "production" tells the frontend NOT to show any environment banner.
VITE_APP_ENV=production
