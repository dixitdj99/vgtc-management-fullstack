# Client Environment — BETA (staging deploy)
# Build with `vite build --mode beta`, or set VITE_APP_ENV=beta in the
# staging deploy's environment.
# Shows an orange "BETA" banner in the UI.
VITE_APP_ENV=beta
